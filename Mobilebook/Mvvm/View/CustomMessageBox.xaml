<Window x:Class="Mobilebook.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mobilebook"
        mc:Ignorable="d"
         Title="Registration" Height="230" Width="250"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border
        Background="#272537"
        CornerRadius="20">
            <StackPanel Orientation="Vertical">
                <TextBlock x:Name="TXT" Margin="0, 80" Text="Увы вас нету в базе❤" Foreground="White" FontSize="20" HorizontalAlignment="Center"/>

               

                <Button x:Name="Close"
                        Content="Закрыть"
                        Margin="0,0,5,0"
                        Width="110"
                        Height="30"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Padding="5"
                        Style="{StaticResource gridButtonClose}"
                        Click="Close_Click"/>

            </StackPanel>
        </Border>
    </Grid>
</Window>
